# Application name
name: ai-hub-gateway-landing-zone
infra:
  provider: bicep
services:
  usageProcessingFunctionApp:
    project: ./src/usage-ingestion-function
    language: dotnet
    host: function